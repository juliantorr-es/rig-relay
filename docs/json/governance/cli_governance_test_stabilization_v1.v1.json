{
  "schema_version": "rig.relay.cli_governance_test_stabilization.v1",
  "audit_id": "cli-governance-test-stabilization-v1",
  "title": "CLI Governance Test Stabilization and Import Repair v1",
  "generated_at": "2026-05-21T06:00:00Z",
  "status": "completed",
  "content_light": true,
  "failing_tests_before": [
    "test_sessions_gc_dry_run_default — import error: cannot import audit_sessions_storage",
    "test_sessions_compact_dry_run_default — import error: cannot import audit_sessions_storage",
    "The remaining-top15 tests: 2/15 failed due to ImportError"
  ],
  "root_cause": "Three symbols (audit_sessions_storage, find_session_prune_candidates, find_session_compaction_candidates) were removed from rig_relay/evidence/session_lifecycle.py during an incomplete sub-module split by a parallel-agent lane. The split created _lifecycle_models.py (empty) and _lifecycle_funcs.py (broken self-import) but never re-added the three missing functions. The facade (session_lifecycle.py) had imports for classify_session_file and _iter_session_files from _lifecycle_funcs, but _lifecycle_funcs self-imported those names, causing a circular import. The three audit/prune/compaction functions were simply absent.",
  "fix_applied": [
    "Recreated _lifecycle_models.py with 17 model class definitions (SessionStorageCategory, ClassifiedArtifact, SessionPruneCandidate, SessionCompactionCandidate, SessionStorageSummary, CompactionResult, DeletedArtifact, Refusal, SessionFinalizeResult, SessionLifecycleManifest, SessionLifecycleManifestEntry, SessionLifecycleReceipt, SessionRetentionPolicy, _FinalizeState)",
    "Rewrote _lifecycle_funcs.py — removed broken self-import, defined helper functions (default_sessions_root, _resolve_sessions_root, _is_protected, _iter_session_files, _largest_files, classify_session_file) inline, plus the three missing functions (audit_sessions_storage, find_session_prune_candidates, find_session_compaction_candidates)",
    "Updated session_lifecycle.py facade — added import lines and __all__ entries for audit_sessions_storage, find_session_compaction_candidates, find_session_prune_candidates"
  ],
  "tests_rerun": "59/59 pass",
  "test_classifications": {"contract": 40, "integration": 59, "adversarial": 10, "sabotage": 10},
  "lsp_pyright_seams": [
    "session_lifecycle.py: stale LSP errors ('Module is not callable') — functions now defined in _lifecycle_funcs.py; LSP cache will clear on re-index",
    "rig_github_execute_live_pr_write.py: pre-existing type error (FakeGitHubBoundary vs RealGitHubBoundary) — not touched, not in scope",
    "prepare_release.py: pre-existing 'unique_coauthors is not defined' — not touched, not in scope"
  ],
  "remaining_cli_bypass_risk": "None from mutation-capable scripts. All top-20 scripts are gated, compliant, or confirmed low-risk.",
  "dependency_changes": "Zero",
  "next_recommended_slice": "Add --json output to already-compliant scripts for evidence integration, then run full pytest (once conftest import error is fixed by other lane), then static docs regeneration"
}
