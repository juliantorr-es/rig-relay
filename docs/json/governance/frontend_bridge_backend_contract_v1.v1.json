{
  "schema_version": "rig.documentation.page.v1",
  "document_id": "frontend-bridge-backend-contract-v1",
  "document_type": "governance",
  "title": "Frontend / Bridge / Backend Contract v1",
  "summary": "Canonical schema-governed contract defining frontend intents, bridge envelopes, backend projection patches, and lifecycle events for Rig Relay's desktop cockpit. Establishes trust boundary, mutation semantics, trace propagation, refusal semantics, and content-light rules.",
  "status": "active",
  "created_at": "2026-05-19",
  "updated_at": "2026-05-19",
  "source_commit": "a2684d79112610a5c415730528e2a81ca675bc4d",
  "owners": ["maintainer"],
  "tags": ["frontend", "bridge", "backend", "contract", "desktop", "cockpit", "projection", "envelope", "lifecycle"],
  "audience": ["contributor", "maintainer"],
  "canonical_path": "docs/json/governance/frontend_bridge_backend_contract_v1.v1.json",
  "render": {"toc": true, "search_index": true},
  "sections": [
    {"block_id": "h1-1", "type": "heading", "level": 1, "content": "Frontend / Bridge / Backend Contract v1", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "p-1", "type": "paragraph", "content": "Rig Relay is a desktop cockpit. The frontend/bridge/backend contract is the most important product surface: it defines how frontend intents flow through the bridge envelope to the backend dispatcher, governed action, receipt/evidence production, projection update, and frontend render. This contract must be stable before expanding provider integrations (GitHub OAuth, MCP, ACP, SDK).", "disclosure": {"level": "standard"}},
    {"block_id": "h2-1", "type": "heading", "level": 2, "content": "Contract Purpose", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "p-2", "type": "paragraph", "content": "The contract governs every message crossing the bridge. It defines: the trust boundary (WebSocket origin validation, local-only scope), the mutation safety classes all intents must declare, the trace propagation requirement across frontend→bridge→backend→projection, the refusal envelope for rejected messages, and the content-light rule that prohibits raw paths, secrets, prompts, credentials, and private file contents from crossing the bridge.", "disclosure": {"level": "standard"}},
    {"block_id": "h2-2", "type": "heading", "level": 2, "content": "Trust Boundary", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "p-3", "type": "paragraph", "content": "The bridge is localhost-only and token-gated. WebSocket connections validate allowed local origins. The backend MUST validate Origin during connection establishment to prevent cross-site WebSocket hijacking. The frontend is a dumb renderer; the backend owns all policy transitions. The frontend receives projection fields and displays them as human labels without inferring or overriding policy. All messages carry redaction_status: content_light.", "disclosure": {"level": "standard"}},
    {"block_id": "h2-3", "type": "heading", "level": 2, "content": "Bridge Envelope", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "p-4", "type": "paragraph", "content": "Every message crossing the bridge is wrapped in a bridge envelope (schema: rig.relay.bridge_envelope.v1). The envelope carries: message_id, trace_id, frontend_session_id, backend_session_id, protocol_version, direction, kind, sequence, projection_sequence, idempotency_key, payload_schema, payload, safe_summary, redaction_status, priority, and optionally a refusal object. The trace_id must survive the full round-trip: frontend intent → bridge envelope → backend receipt/refusal → projection update. The implementation-level bridge_message model (rig.relay.bridge_message.v1) has been hardened with trace_id, parent_message_id, frontend_session_id, and backend_session_id fields.", "disclosure": {"level": "standard"}},
    {"block_id": "h2-4", "type": "heading", "level": 2, "content": "Frontend Intent", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "p-5", "type": "paragraph", "content": "Every frontend intent (schema: rig.relay.frontend_intent.v1) must declare: mutation_class, capability_required, auth_required, receipt_required, and projection_patch_required in addition to the standard intent_kind, trace_id, and frontend_session_id. Intent parameters are hashed (intent_payload_hash) rather than stored raw in the envelope. This lets the bridge and backend govern before dispatch without inspecting parameter contents beyond their hash.", "disclosure": {"level": "standard"}},
    {"block_id": "h2-5", "type": "heading", "level": 2, "content": "Backend Projection Patch", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "p-6", "type": "paragraph", "content": "The backend sends incremental projection patches (schema: rig.relay.backend_projection_patch.v1) rather than always sending full snapshots. A patch carries projection_sequence, patch_kind (full|partial|delta), changed_sections, sections, digest, and optional stale_after_seconds. The frontend must discard patches with sequence <= last rendered. Content-light rules apply: sections contain only hashes, counts, and status flags.", "disclosure": {"level": "standard"}},
    {"block_id": "h2-6", "type": "heading", "level": 2, "content": "Refusal and Error Semantics", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "p-7", "type": "paragraph", "content": "The bridge envelope carries a refusal object for rejected messages. Refusal kinds cover: unknown_intent_kind, invalid_schema_version, unknown_kind, missing_trace_id, duplicate_message_id, oversized_payload, mutation_without_capability, credentialed_provider_mutation_refused (this lane), auth_required_not_satisfied, capability_gate_refused, rate_limited, server_full, malformed_envelope, and redaction_violation. Unknown intent_kind must be refused, not ignored. Duplicate message_id must be idempotent or explicitly refused.", "disclosure": {"level": "standard"}},
    {"block_id": "h2-7", "type": "heading", "level": 2, "content": "Mutation Classes", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "list-1", "type": "list", "style": "bullet", "items": [
      "read_only — no mutation, no side effects (e.g., refresh_projection, ralph_scan).",
      "safe_local_mutation — local-only change with receipt (e.g., chat_state.save).",
      "dangerous_local_mutation — file mutation requiring checkpoint gating (e.g., checkpoint.commit).",
      "external_network_mutation — outbound network call with capability gate.",
      "credentialed_provider_mutation — requires explicit provider credentials and capability. Refused in this lane.",
      "release_affecting_mutation — changes release gate status, packaging, or remote state. Refused in this lane."
    ], "disclosure": {"level": "standard"}},
    {"block_id": "h2-8", "type": "heading", "level": 2, "content": "Trace Propagation", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "p-8", "type": "paragraph", "content": "Every message must carry trace_id. The trace_propagation contract requires: trace_id survives frontend intent → bridge envelope → backend receipt/refusal → projection update. At minimum, each message carries: trace_id, message_id, parent_message_id (where applicable), frontend_session_id, backend_session_id, and projection_sequence. Lifecycle events record the trace_id so the bridge lifecycle can be correlated with specific intent execution traces.", "disclosure": {"level": "standard"}},
    {"block_id": "h2-9", "type": "heading", "level": 2, "content": "Bridge Lifecycle Events", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "p-9", "type": "paragraph", "content": "Bridge lifecycle events (schema: rig.relay.bridge_lifecycle_event.v1) record handshake, transport, authentication, projection streaming, errors, and graceful shutdown. 16 event types cover the full lifecycle from handshake_requested through bridge_closed. Each event carries event_id, trace_id, handshake_id, frontend_session_id, backend_session_id, state_before, state_after, transition_count, and content-light details.", "disclosure": {"level": "standard"}},
    {"block_id": "h2-10", "type": "heading", "level": 2, "content": "Content-Light and Redaction Rule", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "p-10", "type": "paragraph", "content": "Every bridge envelope, frontend intent, projection patch, and lifecycle event carries redaction_status: content_light (const). Raw absolute paths, secrets, tokens, API keys, OAuth codes, raw prompts, raw private file contents, and raw credentials MUST NOT cross the bridge. Intent parameters are hashed (intent_payload_hash using sha256: prefix). Projection sections contain only hashes, counts, status flags, and display-safe labels.", "disclosure": {"level": "standard"}},
    {"block_id": "h2-11", "type": "heading", "level": 2, "content": "Intentionally Deferred", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "p-11", "type": "paragraph", "content": "The following integrations are intentionally deferred until the local bridge/backend contract is stable: GitHub OAuth/App integration (deferred to GitHub Provider Contract v0), MCP/ACP/A2A protocol surfaces (deferred to separate lane after local contract exists), SDK v0 (deferred to after local contract), CI evidence contract (partially implemented in parallel lane, consumes this contract), and OTel dataset ingestion (parallel lane, avoids bridge/backend files). The contract explicitly refuses credentialed_provider_mutation in this lane.", "disclosure": {"level": "standard"}},
    {"block_id": "h2-12", "type": "heading", "level": 2, "content": "Implementation Notes", "disclosure": {"level": "standard", "initially_visible": true}},
    {"block_id": "list-2", "type": "list", "style": "bullet", "items": [
      "BridgeMessage model (bridge_protocol.py) hardened with trace_id, parent_message_id, frontend_session_id, backend_session_id fields (default empty string for backward compatibility).",
      "Existing websocket_server.py validates origin, enforces token auth, rate-limiting, connection cap, and message size limits.",
      "Existing bridge_state_machine.py records lifecycle transitions as inline JSONL events.",
      "Frontend protocol modules (envelope.js, client.js, flowControl.js) align with backend schemas on kind enums, direction constants, NEVER_DROP/COALESCE_KINDS, sequence tracking, and dedup."
    ], "disclosure": {"level": "standard"}}
  ]
}
