# Generated by Contract Compiler Experiment 0
# Generator: jinja2_template_v0
# Contract family: {{ contract_family_id }}
# Schema: {{ schema_id }}
# WARNING: This is a compiler candidate — not hand-written code.
# Do not edit. This file lives in a scratch worktree and is not in the canonical repo.

from __future__ import annotations
{%- for imp in imports %}
{{ imp }}
{%- endfor %}

{% for model in models %}
class {{ model.class_name }}({{ model.base }}):
    model_config = {"extra": "forbid"}

    schema_version: str = "{{ model.schema_version }}"
{%- for field in model.fields %}
    {{ field.name }}: {{ field.type }} = None
{%- endfor %}
{% endfor %}
